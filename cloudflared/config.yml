# Cloudflare Tunnel configuration
# Replace YOUR_TUNNEL_ID with the ID from Zero Trust Dashboard → Tunnels → your tunnel

tunnel: YOUR_TUNNEL_ID
no-autoupdate: true

originRequest:
  keepAliveTimeout: 90s
  keepAliveConnections: 10
  tcpKeepAlive: 30s
  connectTimeout: 30s

// Copy this file to config.js and set the public PocketBase origin.
// config.js is ignored by Git so deployment-specific URLs stay out of the repository.
window.PWATP_CONFIG = Object.freeze({
  pbUrl: "https://pocketbase.example.com"
});
